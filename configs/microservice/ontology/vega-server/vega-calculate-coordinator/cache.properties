# 单节点 docker 部署：关闭 vega-cache（catalog 查询缓存）。
# 该特性需要外部 ClickHouse + MySQL 元数据库（hetu.metastore.db.* / clickhouse），
# 镜像默认的 hetu-metastore.properties 为 hetufilesystem 类型、无 db.* 配置，
# 开启时 MysqlDruidConfig.init 会 Integer.parseInt(null) 崩溃。此处置 false 直接跳过初始化。
catalog-enable=false
